#!/bin/sh

find_this=$1
shift
replace_with=$1
shift
rest=$1

if [ rest == "" ]; then
  ag -l $find_this **/* | xargs sed -i '' "s/$find_this/$replace_with/g"
else
  ag -l $find_this $* | xargs sed -i '' "s/$find_this/$replace_with/g"
fi
